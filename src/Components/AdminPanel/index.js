import React, { Component } from "react";

export default class AdminPanel extends Component {
    render() {
        return (
            <div>
                Logged in as Admin.
            </div>
        );
    }
}